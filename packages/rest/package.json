{
  "name": "@shared/rest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "lint": "tsc --noEmit",
    "test": "node --import tsx --test $(find src -type f \\( -name '*.test.ts' -o -name '*.e2e.test.ts' \\))"
  },
  "dependencies": {
    "@shared/database": "*",
    "@shared/types": "*",
    "@trpc/server": "^11",
    "superjson": "^2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@shared/tsconfig": "*",
    "tsx": "^4.20.6",
    "typescript": "^5.7"
  }
}
