name: Build Queue

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-queue:
    name: Build Queue
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Type Check
        run: npm run type-check

      - name: Lint
        run: npm run lint

      - name: Build Queue
        run: npm run build --workspace @app/queue
